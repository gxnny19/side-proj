<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jgg.side_proj.repository.UserRepository">

    <insert id="save" parameterType="UserEntity">
        INSERT INTO users (username, password, name, created_at)
        VALUES (#{username}, #{password}, #{name}, #{createdAt})
    </insert>

    <select id="findByUsername" parameterType="String" resultType="UserEntity">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <select id="existsByUsername" parameterType="String" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE username = #{username}
    </select>

</mapper>